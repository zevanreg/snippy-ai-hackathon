{
  "version": "2.0.0",
  "tasks": [
    // No local development tasks - deploy directly to Azure
    // Use Azure Functions Core Tools for remote deployment:
    // func azure functionapp publish <function-app-name>
  ]
}
